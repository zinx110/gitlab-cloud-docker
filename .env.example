# GitLab Environment Variables
# Copy this file to .env and update with your values

# Initial root password (optional - defaults to MySecurePassword123! if not set)
INITIAL_PASSWORD=YourSecurePassword123!

# External IP address (optional - defaults to localhost if not set)
# For localhost testing: Leave commented out or don't set
# For cloud deployment: Set to your server's public IP or domain
# EXTERNAL_IP=YOUR-SERVER-IP-HERE

# Examples:
# EXTERNAL_IP=54.123.45.67        # AWS EC2 public IP
# EXTERNAL_IP=143.198.123.45      # DigitalOcean droplet IP
# EXTERNAL_IP=gitlab.yourdomain.com  # Custom domain

# Note: 
# - If EXTERNAL_IP is not set, GitLab will use "localhost"
# - Change INITIAL_PASSWORD to something secure for production
# - Never commit the actual .env file to version control
